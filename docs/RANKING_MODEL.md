# Ranking model

Ranking is a versioned data model, not an AI system. It preserves component scores, confidence, reasoning, evidence, score timestamp, and an optional account target. Rights and moderation risks remain separate component scores and are never concealed by `overall_score`. Platform- and account-specific weighting is deferred.
