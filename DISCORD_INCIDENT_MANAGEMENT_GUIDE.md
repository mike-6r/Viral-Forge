# Incident Management

Create internal records with `/admin incident-create`. Public updates must be intentionally prepared and must never include infrastructure details, credentials, IP addresses, provider tokens, or customer data. Announcement drafts are separate from publishing and require explicit confirmation.
