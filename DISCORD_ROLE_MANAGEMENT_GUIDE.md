# Role Management

Only configured notification roles are self-service. Customer (including Verified Customer), subscription, staff, workspace, and operational roles must be assigned by an authorized process. Temporary grants are limited to the configured `Trial User` role, are persisted with source and expiry, and always check role hierarchy and bot manageability.
